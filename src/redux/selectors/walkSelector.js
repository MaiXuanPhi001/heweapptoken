export const ranLimitSelector = state => state.walk.ranLimit

export const latiudeStartSelector = state => state.walk.latitudeStart

export const longtiudeStartSelector = state => state.walk.longitudeStart

export const totalRunSelector = state => state.walk.totalRun

export const runDetailSelector = state => state.walk.detailRun

export const loadingSelector = state => state.walk.loading